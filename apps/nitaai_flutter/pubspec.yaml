name: nitaai_flutter
description: NitaAi Flutter client
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.6.0
  
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0

  google_generative_ai: ^0.4.0

  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
